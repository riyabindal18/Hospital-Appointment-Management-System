<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Appointments</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <%- include('../partials/header') %>

    <div class="container">
        <div class="page-header">
            <h1><i class="fas fa-calendar-alt"></i> Manage Appointments</h1>
            <p>View and manage all appointment bookings in the system</p>
        </div>

        <% if (appointments.length === 0) { %>
            <div class="empty-state">
                <div class="empty-icon"><i class="fas fa-calendar-alt"></i></div>
                <h3>No appointments found</h3>
                <p>All appointment bookings will appear here</p>
            </div>
        <% } else { %>
            <div class="data-table-container">
                <table class="data-table">
                    <thead>
                        <tr>
                            <th><span class="header-icon"><i class="fas fa-user"></i></span> Patient</th>
                            <th><span class="header-icon"><i class="fas fa-user-md"></i></span> Doctor</th>
                            <th><span class="header-icon"><i class="fas fa-stethoscope"></i></span> Specialization</th>
                            <th><span class="header-icon"><i class="fas fa-calendar"></i></span> Date & Time</th>
                            <th><span class="header-icon"><i class="fas fa-chart-line"></i></span> Status</th>
                            <th><span class="header-icon"><i class="fas fa-cogs"></i></span> Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% appointments.forEach(apt => { %>
                            <tr>
                                <td class="patient-name"><%= apt.patientName %></td>
                                <td>
                                    <%= apt.doctorId ? apt.doctorId.name : 'Unknown Doctor' %>
                                    <br><small style="color: #666;">ID: <%= apt.doctorId ? apt.doctorId._id : apt.doctorId %></small>
                                </td>
                                <td>
                                    <span class="specialization-badge"><%= apt.doctorId ? apt.doctorId.specialization : 'N/A' %></span>
                                </td>
                                <td>
                                    <div class="date-info">
                                        <div class="date"><%= apt.date.toLocaleDateString() %></div>
                                        <div class="time"><%= apt.date.toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}) %></div>
                                    </div>
                                </td>
                                <td>
                                    <span class="status-badge <%= apt.status.toLowerCase() %>">
                                        <%= apt.status %>
                                    </span>
                                </td>
                                <td>
                                    <form action="/admin/appointments/update" method="POST" class="status-form">
                                        <input type="hidden" name="appointmentId" value="<%= apt._id %>">
                                        <div class="form-group-inline">
                                            <select name="status" class="status-select">
                                                <option value="Pending" <%= apt.status === 'Pending' ? 'selected' : '' %>>Pending</option>
                                                <option value="Confirmed" <%= apt.status === 'Confirmed' ? 'selected' : '' %>>Confirmed</option>
                                                <option value="Cancelled" <%= apt.status === 'Cancelled' ? 'selected' : '' %>>Cancelled</option>
                                            </select>
                                            <button type="submit" class="btn-action">Update</button>
                                        </div>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } %>

        <div class="navigation">
            <a href="/admin/dashboard" class="btn-secondary">
                <span class="btn-icon"><i class="fas fa-arrow-left"></i></span>
                Back to Dashboard
            </a>
        </div>
    </div>

    <style>
        .page-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            border-radius: 15px;
            margin-bottom: 25px;
            text-align: center;
        }
        .page-header h1 {
            margin: 0 0 10px 0;
            font-size: 2.2em;
        }
        .page-header p {
            margin: 0;
            opacity: 0.9;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        }
        .empty-icon {
            font-size: 4em;
            margin-bottom: 20px;
        }
        .empty-state h3 {
            color: #6c757d;
            margin-bottom: 10px;
        }
        .empty-state p {
            color: #adb5bd;
        }

        .data-table-container {
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            overflow: hidden;
            margin-bottom: 25px;
        }

        .data-table {
            width: 100%;
            border-collapse: collapse;
        }

        .data-table thead {
            background: linear-gradient(135deg, #00796b, #009688);
            color: white;
        }

        .data-table th {
            padding: 15px 12px;
            text-align: left;
            font-weight: 600;
            font-size: 0.95em;
        }

        .data-table td {
            padding: 15px 12px;
            border-bottom: 1px solid #e9ecef;
            vertical-align: top;
        }

        .data-table tbody tr:hover {
            background: #f8f9fa;
        }

        .header-icon {
            margin-right: 8px;
        }

        .patient-name {
            font-weight: 600;
            color: #2c3e50;
        }

        .specialization-badge {
            background: #e3f2fd;
            color: #1976d2;
            padding: 4px 8px;
            border-radius: 12px;
            font-size: 0.85em;
            font-weight: 600;
        }

        .date-info {
            line-height: 1.4;
        }
        .date {
            font-weight: 600;
            color: #2c3e50;
        }
        .time {
            font-size: 0.9em;
            color: #6c757d;
        }

        .status-badge {
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.85em;
            font-weight: 600;
            text-transform: uppercase;
        }

        .status-badge.pending {
            background: #fff3cd;
            color: #856404;
        }

        .status-badge.confirmed {
            background: #d1ecf1;
            color: #0c5460;
        }

        .status-badge.cancelled {
            background: #f8d7da;
            color: #721c24;
        }

        .status-form {
            display: flex;
            align-items: center;
        }

        .form-group-inline {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .status-select {
            padding: 6px 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 0.9em;
        }

        .btn-action {
            padding: 6px 12px;
            background: #00796b;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 0.9em;
            font-weight: 600;
            transition: background 0.3s ease;
        }

        .btn-action:hover {
            background: #005a4f;
        }

        .btn-secondary {
            background: #6c757d;
            color: white;
            padding: 12px 25px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s ease;
        }

        .btn-secondary:hover {
            background: #5a6268;
            transform: translateY(-2px);
        }

        .navigation {
            text-align: center;
        }

        @media (max-width: 768px) {
            .data-table-container {
                overflow-x: auto;
            }
            .data-table {
                min-width: 700px;
            }
            .form-group-inline {
                flex-direction: column;
                align-items: stretch;
                gap: 5px;
            }
            .status-select {
                width: 100%;
            }
        }
    </style>

    <%- include('../partials/footer') %>
</body>
</html>